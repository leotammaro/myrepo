import { Image } from "@chakra-ui/react";
import React from "react";
import menu from "../svg/menu.svg";

function MenuButtonLogo() {
  return <Image src={menu} w={30} h={30} />;
}

export default MenuButtonLogo;
